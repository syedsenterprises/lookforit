# Contact Responses Folder

This folder is reserved for manually exported contact-form responses.

On GitHub Pages, form submissions cannot be stored directly on the server. The current contact form is designed to work with an external form handler, and exported responses can be stored here for record keeping.
